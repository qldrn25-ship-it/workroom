# WORKROOM Windows clean build

GitHub Actions builds the Windows distribution on a clean Microsoft-hosted runner and scans both the unpacked application and final EXE artifacts with Microsoft Defender before publishing artifacts. SHA-256 checksums are included with the build.

The source archive used by the workflow is `WORKROOM_Standalone_v16_taskbar_WORKROOM.zip`.
